#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Do any other automated setup that you need to do here
[ ! -f config.json ] && cp config.example.json config.json
./exe/workday config setup
ruby -e "require 'tty-prompt'; require 'pastel'; TTY::Prompt.new.ok '✅ All set!'"
